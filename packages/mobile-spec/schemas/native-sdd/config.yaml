schema: native-sdd
rules:
  design:
    - 生成 artifact 前，根据 CLAUDE.md、AGENT.md 中的路由表按任务范围读取 docs/* 对应文件获取完整上下文。
  postArchive:
    - 归档后运行 Skill `opsx-sync-context-native` 触发上下文同步，将变更设计信息同步回 docs/* 对应文件。
