# Proposal

<!--
以下是生成约束，不在 Proposal 渲染结果中展示：
- **目标**：澄清需求并形成可确认的范围基线，只回答为什么做、做什么、不做什么。
- **权威输入**：PRD 与用户 / PM 的明确确认是需求事实来源；现状代码只按下述条件补充现状，不得反推需求；技术方案、接口文档、其他实现方案和设计稿不得作为需求事实来源。
- **内容边界**：Proposal 只保留为什么做、做什么、不做什么，以及未决问题的处置结果。受影响页面、Spec ID、模块、产品组件、用户故事、具体需求、业务规则、验收场景和异常边界直接写入对应 Spec；接口、字段、协议、代码模块、Pod、状态管理、技术选型及实现方案只供 Design 使用，不得写入 Proposal。
- **下游约束**：Specs 在 Proposal 的“做什么 / 不做什么”边界内，直接依据 PRD 与用户 / PM 确认划分页面并写入详细需求和行为契约。
- **就绪与阻断**：`ready` 表示为什么做、做什么、不做什么已明确，且每条未决问题均已处置；未决问题仍为 `待确认` 时标记为 `blocked`。处置不要求当场得到答案：RD 明确确认后续补充、同时记录补充安排和确认依据，也视为已处置。
-->

## 1. 输入材料与相关人

<!--
用户在 Proposal 阶段提供的材料必须全部登记。技术方案、接口文档、其他实现方案和设计稿链接只登记原始链接 / 标识与材料类型，不打开、读取或解析，留到 Design 阶段读取；直接提供的设计稿截图必须在 Proposal 阶段读取，作为 PRD 改动点的样式补充，样式以截图为准，文案和逻辑以 PRD 为准；未提供的固定类型写“未提供”。
现状代码不是 Proposal 的必读输入。仅当需求涉及存量行为、问题修复、兼容边界，或 PRD 无法说明现状时，才最小范围读取并记录用户可见的现状结论；不得从代码反推需求或产品层级。
-->

| 类型 | 链接 / 标识 | 后续用途 |
|------|-------------|---------|
| 补充链接 | | Proposal 需求来源 |
| PRD 文档 | | Proposal 需求来源 |
| 用户 / PM 补充确认 | | Proposal 需求来源 |
| 设计稿链接 | | 仅登记；Design 阶段读取 |
| 设计稿截图 | | Proposal 阶段必读；作为 PRD 改动点的样式补充 |
| 后端技术方案 | | 仅登记；Design 阶段读取 |
| 接口文档（可选） | | 仅登记；Design 阶段读取 |
| 现状代码（按需） | | Proposal 补充现状；Design 实现证据 |
| 其他用户提供材料（可选） | | 按材料类型注明 |

- PM：
- 客户端 owner：
- Backend owner：
- QA owner：

## 2. 需求结论

### 2.1 为什么做

<!-- 说明产品背景、用户问题和预期价值，不写实现方案。 -->

### 2.2 做什么

<!-- 只概括本次交付范围；模块、产品组件和行为细节直接写入对应 Spec。 -->

- <本次要交付的需求范围>

### 2.3 不做什么

- <明确排除的需求范围>

## 3. 未决问题

<!-- 无未决问题时保留表头并删除示例行。 -->
| 问题 | 影响范围 | Owner | 处置状态 | 处置结论 | 确认依据 |
|------|----------|-------|----------|----------|----------|
|      |          |       | 待确认 / 已解决 / 已确认后续补充 | 答案，或由谁在何时补充 | PRD 章节，或 RD 确认记录 |

## 4. Proposal 就绪状态

### 4.1 RD 重点确认

- [ ] 为什么做、做什么、不做什么已确认
- [ ] 每条未决问题均已解决，或已由 RD 确认后续补充并记录依据

<!--
Agent 自检（不在 Proposal 渲染结果中展示）：
- [ ] 用户在 Proposal 阶段提供的材料已全部登记并注明后续用途
- [ ] 受影响页面、Spec ID、模块、产品组件、用户故事、具体需求、业务规则和验收场景未写入 Proposal
- [ ] 文档未混入接口、代码结构或其他实现方案
- [ ] 未决问题表不存在 `待确认`，且每条处置均有结论和确认依据

默认保持 blocked；4.1 全部满足时改为 ready。Agent 自检项由 Agent 自行修正，不增加 RD 的 Specs 评审负担。
-->
`status: blocked`
